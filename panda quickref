df_no_duplicates = df.drop_duplicates()
 of four columns need only thee quni
df_unique = df.drop_duplicates(subset=['A', 'B', 'C'])
# Sorting by three columns: col1, col2, and col3
sorted_df = df.sort_values(by=['col1', 'col2', 'col3'])
